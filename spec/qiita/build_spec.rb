require 'spec_helper'

describe Qiita::Build do
  it 'has a version number' do
    expect(Qiita::Build::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
